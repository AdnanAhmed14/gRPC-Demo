This is a small POC on gRPC 

I have commented the .proto file since w have already created the java files which we need so no need of generating it again it will cause error

To test this project just start the GrpcServer and then the GrpcClient
,it will output the hardcode values 